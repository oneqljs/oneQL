module.exports = {
  redisName: 'xxx',
  xconfig: ['properties'],
  Env: 'fws', // support dev, fat, uat, pro
  // 'SOA.Timeout': 100 // [optional] soa全局的timeout，可以被单独调用的timeout覆盖
  port: 3600, // 默认启动端口
  middle: ''
}
